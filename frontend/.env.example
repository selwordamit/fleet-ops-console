# Backend Socket.IO origin the browser connects to for live telemetry.
# Socket.IO uses the /socket.io/ path (not handled by the Vite /api proxy), so
# this targets the backend directly. Copy this file to .env.local and adjust if
# your backend runs elsewhere. .env.local is gitignored and never committed.
VITE_SOCKET_URL=http://localhost:8000